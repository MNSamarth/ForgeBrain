/**
 * Configuration placeholders for every external system ForgeBrain depends on. See
 * docs/CONFIGURATION.md for the full rationale and the "no credentials, structure only"
 * principle every class here follows — none of these types hold a real project ID, bucket
 * name, or credential; see {@code src/main/resources/application.yml} for the (also
 * placeholder) default values.
 */
package com.forgebrain.backend.config;
