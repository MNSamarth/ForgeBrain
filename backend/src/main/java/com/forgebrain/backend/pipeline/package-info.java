/**
 * Orchestration contracts for running one topic through the full fourteen-stage pipeline
 * described in docs/PIPELINE.md. Nothing here implements a stage itself — see {@link
 * com.forgebrain.backend.services} for the per-stage contracts this package sequences.
 */
package com.forgebrain.backend.pipeline;
