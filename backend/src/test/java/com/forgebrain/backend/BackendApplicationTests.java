package com.forgebrain.backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test confirming the application context loads with every {@code config},
 * {@code controllers}, and other Spring-managed package wired correctly. This is a genuine,
 * minimal test of the actual scaffold — not a placeholder for pipeline logic, since none
 * exists yet to test.
 */
@SpringBootTest
class BackendApplicationTests {

    @Test
    void contextLoads() {
    }
}
